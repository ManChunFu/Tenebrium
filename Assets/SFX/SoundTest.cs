using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SoundTest : MonoBehaviour
{
    /*void Start()
    {
        //Play sound event on gameobject
        SoundManager.Sound.SFX.DoorOpen.Post(gameObject);
    }

    //function for playing sound
    public void Explosion()
    {
        SoundManager.Sound.SFX.Explosion.Post(gameObject);
    }

    //Sound for physics enabled objects
    private void OnCollisionEnter(Collision collision)
    {
        //Set force parameter then play sound
        if (collision.relativeVelocity.magnitude > 2 && collision.relativeVelocity.magnitude < 6)
        {
            //AkSoundEngine.SetRTPCValue("Name", value);
            AkSoundEngine.SetRTPCValue("Force", collision.relativeVelocity.magnitude, gameObject);
            SoundManager.Sound.SFX.CollisionPorsline.Post(gameObject);
        }

        //Play breaking sound
        else
        {
            SoundManager.Sound.SFX.DestructiblePorsline.Post(gameObject);
        }
    }*/
}
